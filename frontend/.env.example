# Use 127.0.0.1 — another Docker service often binds localhost:8000 on macOS
VITE_API_URL=http://127.0.0.1:8000

# Production (set in Vercel → Environment Variables, then redeploy frontend):
# VITE_API_URL=https://memoryweave-r6r4.onrender.com
